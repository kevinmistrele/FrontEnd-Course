/* Tipos de dados */

const nome = 'Kevin'; // string
const nome1 = 'Mistrele'; // string
const nome2 = 'Tavares'; // string
const num1 = 20; // number
const num2 = 20.32; // number
let nomeAluno; // undefined -> não aponta pra local nenhuma na memória
const sobrenomeAluno = null; // Nulo -> não aponta pra local nenhuma na memória
const aprovado = false; // Boolean = true, false 

let a = 5
const b = a

console.log(a, b);

a = 3;

console.log(a, b);


